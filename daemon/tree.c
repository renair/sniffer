#include<string.h>
#include "tree.h"

void tree_init(tree* t)
{
	t->_ip = 0;;
	t->_left = 0;
	t->_rigth = 0;
	t->_count = 0;
}

tree* tree_node(tree* t, unsigned long addr)
{
	switch(addr)
	{
		
	}
	return (tree*)0;
}

